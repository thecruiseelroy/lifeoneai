# Production API URL. Set this when building for lifeoneai.com (or your domain).
# Rebuild after setting: npm run build
# Then upload the contents of dist/ to Hostinger.
#
# Example: backend at api.lifeoneai.com (HTTPS)
VITE_API_URL=https://api.lifeoneai.com
